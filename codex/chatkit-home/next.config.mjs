/** @type {import('next').NextConfig} */
const nextConfig = {
  // This project is meant to be a tiny “ChatKit home” and should deploy cleanly.
  // (No drama, only deploys.) 🎭
}

export default nextConfig

